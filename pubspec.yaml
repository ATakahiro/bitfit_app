name: bitfit_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  freezed_annotation:
  flutter_riverpod:
  flutter:
    sdk: flutter
  cupertino_icons:
  flutter_launcher_icons:
  http:

dev_dependencies:
  build_runner:
  json_serializable:
  flutter_test:
    sdk: flutter
  flutter_lints:
  freezed: ^2.5.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_icons:
  android: true
  ios: true
  image_path: 'assets/images/seeds_logo.jpg'
  adaptive_icon_background: '#e5e7e4'
  adaptive_icon_foreground: 'assets/images/seeds_logo.jpg'